# Personal Knowledge Base Frontend

Vue 3 + Vite frontend for the personal knowledge base assistant.

```bash
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8080`.
